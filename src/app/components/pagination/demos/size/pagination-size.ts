import { Component } from '@angular/core';

@Component({
  selector: 'app-pagination-size',
  templateUrl: './pagination-size.html',
  styleUrls: ['./pagination-size.scss']
})
export class PaginationSizeComponent {
  currentPage = 3;
}
